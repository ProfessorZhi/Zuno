# Architecture Refoundation 未决问题

状态：`UNKNOWN` / 不阻塞本轮文档迁移

1. 未来是否仍保留若干独立专题文档，必须由下一轮 Architecture Red/Blue 决定；本轮不冻结最终服务数量或模块数量。
2. `docs/project/architecture/architecture.md` 当前 Target 内容是否需要进一步按新的产品问题重写，属于后续 Architecture Reconstruction，不在本轮路径迁移中解决。
3. 旧专题中哪些 Contract 会被下一轮吸收为总体架构共享 Contract，需在不修改旧 Round immutable artifacts 的前提下逐项审计。
4. `docs/verification/interview-qa/` 是非规范性验证语料，是否另建独立 taxonomy 不影响本轮 Canonical 文档收敛。
5. 历史项目中精确客户机构、原始产品名、试点部署位置、QA 协议、Bad Case 和个人代码级 Ownership 仍按 `UNKNOWN` / `USER_PARTIAL_RECALL` 管理。
