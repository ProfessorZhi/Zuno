# Question-by-question Semantic Audit

Original Class 只作历史对照；Derived Class 来自本题场景语义。

| ID | Lens | Original Class | Attack-time Class | Post-round Class | Finding State | Secondary Gaps | Changed | Closure Class Rationale | Post-round Reason |
|---|---|---|---|---|---|---|---|---|---|
| Q001 | 00 Overall Architecture | A | A | I | REMAINS_OPEN | I | NO | 场景“DomainVersion D31 已提交、Checkpoint 仍在 D30、Graph Projection 仍在 D29”使 跨 的 Owner/authority 在 恢复时谁是真相，谁负责把三个版本重新对齐 时不唯一；Round-005 的同步把边界写清后，剩余成熟度 Gate 是 I 对应的实现或测量，而不是继续接受一个含糊的架构 Contract。 | DomainVersion D31 已提交、Checkpoint 仍在 D30、Graph Projection 仍在 D29 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q002 | 00 Overall Architecture | A | A | I | REMAINS_OPEN | I | NO | 场景“Host 和 Native Runtime 同时提交同一 Matter 的 FindingProposal”使 external 的 Owner/authority 在 Admission 如何阻止两条路径形成两个事实版本 时不唯一；Round-005 的同步把边界写清后，剩余成熟度 Gate 是 I 对应的实现或测量，而不是继续接受一个含糊的架构 Contract。 | Host 和 Native Runtime 同时提交同一 Matter 的 FindingProposal 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q003 | 00 Overall Architecture | A | I | I | REMAINS_OPEN | NONE | YES | 场景“Domain Transaction 成功但 Outbox 发布失败，重启后 Job 重投”对应的 business Contract 已能由 `docs/project/architecture/architecture.md` 说明，问题中的 恢复先看 Domain、Outbox 还是 Queue ACK，为什么 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Domain Transaction 成功但 Outbox 发布失败，重启后 Job 重投 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q004 | 00 Overall Architecture | A | I | I | REMAINS_OPEN | X | YES | 场景“Tool EffectReceipt 显示 outcome_unknown，而 Runtime 停在调用前”对应的 unknown Contract 已能由 `docs/project/architecture/architecture.md` 说明，问题中的 Coordinator 如何决定 reconcile、retry 或 review 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Tool EffectReceipt 显示 outcome_unknown，而 Runtime 停在调用前 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q005 | 00 Overall Architecture | A | I | I | REMAINS_OPEN | I | YES | 场景“Knowledge 返回带旧 ACL 的 EvidenceCandidate，Domain 已撤权”对应的 cross-layer Contract 已能由 `docs/project/architecture/architecture.md` 说明，问题中的 哪一层必须阻断候选进入计划，谁拥有最终拒绝权 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Knowledge 返回带旧 ACL 的 EvidenceCandidate，Domain 已撤权 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q006 | 00 Overall Architecture | A | I | I | REMAINS_OPEN | NONE | YES | 场景“两个 Service 都缓存 Matter Snapshot，版本更新只通知到一个”对应的 snapshot Contract 已能由 `docs/project/architecture/architecture.md` 说明，问题中的 怎样检测并避免旧 Snapshot 继续规划 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 两个 Service 都缓存 Matter Snapshot，版本更新只通知到一个 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q007 | 00 Overall Architecture | A | I | I | REMAINS_OPEN | NONE | YES | 场景“服务拆分后一个请求跨 Domain、Runtime、Tool 三个失败域”对应的 failure Contract 已能由 `docs/project/architecture/architecture.md` 说明，问题中的 哪些结果可以重试，哪些只能保留未知状态 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 服务拆分后一个请求跨 Domain、Runtime、Tool 三个失败域 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q008 | 00 Overall Architecture | A | I | I | REMAINS_OPEN | NONE | YES | 场景“Reviewer 修改 Finding 后旧 Plan 仍有两个 Worker 在运行”对应的 review Contract 已能由 `docs/project/architecture/architecture.md` 说明，问题中的 人工决定怎样成为新的规划屏障并阻止旧分支提交 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Reviewer 修改 Finding 后旧 Plan 仍有两个 Worker 在运行 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q009 | 00 Overall Architecture | A | I | I | REMAINS_OPEN | NONE | YES | 场景“Model、Graph 和 Memory Provider 依次替换，结果仍要可审计”对应的 provider Contract 已能由 `docs/project/architecture/architecture.md` 说明，问题中的 平台如何证明语义契约未被 Provider 状态绑架 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Model、Graph 和 Memory Provider 依次替换，结果仍要可审计 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q010 | 00 Overall Architecture | A | E | E | EVIDENCE_PENDING | I | YES | 场景“A/B/C 质量接近但 Native Runtime 成本明显更高”已有可执行的 reversal Contract，问题中的 哪个复杂度先降级，谁拥有撤回决定 真正缺少的是对质量、效率、成本或归因的受控测量；因此第一阻塞 Gate 是 E，而不是把未证明的收益误写成 I 或 A。 | A/B/C 质量接近但 Native Runtime 成本明显更高 的首要架构判断已经完成；当前仍开放的是 E，状态为 EVIDENCE_PENDING，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q011 | 00 Overall Architecture | I | A | NONE | RESOLVED_IN_ROUND | NONE | YES | 场景“旧 11 模块文档与新服务边界对同一 Owner 给出不同说法”使 taxonomy 的 Owner/authority 在 阅读和实现时哪个文档是唯一事实源 时不唯一；Round-005 的同步把边界写清后，剩余成熟度 Gate 是 NONE 对应的实现或测量，而不是继续接受一个含糊的架构 Contract。 | 旧 11 模块文档与新服务边界对同一 Owner 给出不同说法 的架构歧义已由 Round-005 Canonical Sync 消除；该问题保留为 immutable attack record，当前没有同类开放 Closure Class。 |
| Q012 | 00 Overall Architecture | I | X | X | DEFERRED | I | YES | 场景“滚动发布中旧 Worker 只能读旧 Checkpoint，新 Worker 已接管队列”的 deployment/runtime 设计可以说明，问题中的 谁决定兼容、隔离、回滚或人工接管 只有在真实 Provider、Sandbox、滚动部署或资格环境中才能取得证据；因此第一阻塞 Gate 是 X，代码缺口只能作为 secondary gap。 | 滚动发布中旧 Worker 只能读旧 Checkpoint，新 Worker 已接管队列 的首要架构判断已经完成；当前仍开放的是 X，状态为 DEFERRED，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q013 | 01 Product Surface | I | I | I | REMAINS_OPEN | NONE | NO | 场景“用户上传新证据后打开昨天的 WorkProduct”对应的 product Contract 已能由 `docs/project/product/product-architecture.md` 说明，问题中的 界面如何解释 stale、可继续使用的部分和必须复核的部分 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 用户上传新证据后打开昨天的 WorkProduct 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q014 | 01 Product Surface | I | A | I | REMAINS_OPEN | I | YES | 场景“Reviewer A 接受 Finding，Reviewer B 随新证据拒绝它”使 review 的 Owner/authority 在 最终 HumanDecision 如何排序，旧 WorkProduct 是否作废 时不唯一；Round-005 的同步把边界写清后，剩余成熟度 Gate 是 I 对应的实现或测量，而不是继续接受一个含糊的架构 Contract。 | Reviewer A 接受 Finding，Reviewer B 随新证据拒绝它 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q015 | 01 Product Surface | I | I | I | REMAINS_OPEN | NONE | NO | 场景“External Host 提交 Run 后断线，用户重新进入 Matter”对应的 async Contract 已能由 `docs/project/product/product-architecture.md` 说明，问题中的 如何恢复 Run、MatterVersion 和权限，而不是依赖聊天记录 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | External Host 提交 Run 后断线，用户重新进入 Matter 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q016 | 01 Product Surface | I | I | I | REMAINS_OPEN | NONE | NO | 场景“解析只完成部分页面但其中一页已有引用”对应的 partial Contract 已能由 `docs/project/product/product-architecture.md` 说明，问题中的 产品怎样显示部分可用范围，避免把 partial 当 complete 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 解析只完成部分页面但其中一页已有引用 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q017 | 01 Product Surface | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Reviewer 驳回 Finding，但相关 Evidence 仍被另一项任务使用”对应的 rejection Contract 已能由 `docs/project/product/product-architecture.md` 说明，问题中的 驳回是删除候选、禁止复用还是触发新分析 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Reviewer 驳回 Finding，但相关 Evidence 仍被另一项任务使用 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q018 | 01 Product Surface | I | I | I | REMAINS_OPEN | NONE | NO | 场景“导出 WorkProduct 的瞬间出现新 Conflict”对应的 delivery Contract 已能由 `docs/project/product/product-architecture.md` 说明，问题中的 导出是否阻止、标记版本，或创建新的 immutable 产物 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 导出 WorkProduct 的瞬间出现新 Conflict 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q019 | 02 Input / Document Ingestion | I | I | I | REMAINS_OPEN | NONE | NO | 场景“同一 PDF 的内容 hash 相同但上传者和 Matter 不同”对应的 document Contract 已能由 `docs/project/knowledge/knowledge-evidence-architecture.md` 说明，问题中的 Document identity、权限和幂等键如何同时判断 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 同一 PDF 的内容 hash 相同但上传者和 Matter 不同 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q020 | 02 Input / Document Ingestion | I | I | I | REMAINS_OPEN | NONE | NO | 场景“OCR Worker 在页 40 完成后崩溃，页 1–39 已有临时 Chunk”对应的 partial Contract 已能由 `docs/project/knowledge/knowledge-evidence-architecture.md` 说明，问题中的 重试从哪个阶段开始，谁可以发布可检索版本 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | OCR Worker 在页 40 完成后崩溃，页 1–39 已有临时 Chunk 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q021 | 02 Input / Document Ingestion | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Parser-v3 已发布 span，Parser-v4 改变页码映射”对应的 citation Contract 已能由 `docs/project/knowledge/knowledge-evidence-architecture.md` 说明，问题中的 旧 Citation 如何保持 lineage，何时必须重新生成 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Parser-v3 已发布 span，Parser-v4 改变页码映射 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q022 | 02 Input / Document Ingestion | I | A | I | REMAINS_OPEN | I | YES | 场景“同一 DocumentVersion 的两个 Parser 并发完成且质量不同”使 publication 的 Owner/authority 在 谁只能提交 Proposal，谁有权发布 Projection 时不唯一；Round-005 的同步把边界写清后，剩余成熟度 Gate 是 I 对应的实现或测量，而不是继续接受一个含糊的架构 Contract。 | 同一 DocumentVersion 的两个 Parser 并发完成且质量不同 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q023 | 02 Input / Document Ingestion | I | I | I | REMAINS_OPEN | NONE | NO | 场景“上传取消发生在 Object Store 写入后、Domain Commit 前”对应的 cancellation Contract 已能由 `docs/project/knowledge/knowledge-evidence-architecture.md` 说明，问题中的 孤儿 Artifact 如何标记、清理和防止变成业务事实 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 上传取消发生在 Object Store 写入后、Domain Commit 前 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q024 | 02 Input / Document Ingestion | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Ingestion Job 重复投递且两个 Worker 都开始 Embedding”对应的 duplicate Contract 已能由 `docs/project/knowledge/knowledge-evidence-architecture.md` 说明，问题中的 Job identity、stage idempotency 和发布门如何协作 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Ingestion Job 重复投递且两个 Worker 都开始 Embedding 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q025 | 02 Input / Document Ingestion | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Parser 升级后旧版本正在被检索，新版本只完成一半”对应的 parser Contract 已能由 `docs/project/knowledge/knowledge-evidence-architecture.md` 说明，问题中的 检索使用哪个 published generation，如何回滚 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Parser 升级后旧版本正在被检索，新版本只完成一半 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q026 | 03 Knowledge / Agentic GraphRAG | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Graph edge 来自已撤回的 DocumentVersion，Hybrid 仍返回新版本”对应的 projection Contract 已能由 `docs/project/knowledge/knowledge-evidence-architecture.md` 说明，问题中的 Retrieval Gate 如何拒绝 stale edge 并保留可解释降级 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Graph edge 来自已撤回的 DocumentVersion，Hybrid 仍返回新版本 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q027 | 03 Knowledge / Agentic GraphRAG | I | E | E | EVIDENCE_PENDING | I | YES | 场景“一个 Claim 需要三份材料共同证明，单个 top-k 都不完整”已有可执行的 multi-evidence Contract，问题中的 Evidence Sufficiency 在检索、Join 还是 Domain Gate 判断 真正缺少的是对质量、效率、成本或归因的受控测量；因此第一阻塞 Gate 是 E，而不是把未证明的收益误写成 I 或 A。 | 一个 Claim 需要三份材料共同证明，单个 top-k 都不完整 的首要架构判断已经完成；当前仍开放的是 E，状态为 EVIDENCE_PENDING，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q028 | 03 Knowledge / Agentic GraphRAG | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Exact Statute 查询被 Graph 改写后召回相邻法条”对应的 conditional Contract 已能由 `docs/project/knowledge/knowledge-evidence-architecture.md` 说明，问题中的 QueryClass 如何选择 Lexical/Hybrid，谁阻止错误扩展 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Exact Statute 查询被 Graph 改写后召回相邻法条 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q029 | 03 Knowledge / Agentic GraphRAG | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Agent 选择了错误 Matter Scope，但索引返回了相关文本”对应的 scope Contract 已能由 `docs/project/knowledge/knowledge-evidence-architecture.md` 说明，问题中的 ACL 和 Scope 如何 fail closed 并留下拒绝证据 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Agent 选择了错误 Matter Scope，但索引返回了相关文本 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q030 | 03 Knowledge / Agentic GraphRAG | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Rerank Provider 超时，初始候选仍在缓存中”对应的 typed Contract 已能由 `docs/project/knowledge/knowledge-evidence-architecture.md` 说明，问题中的 系统返回 no_evidence、降级结果还是 blocked，怎样区分 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Rerank Provider 超时，初始候选仍在缓存中 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q031 | 03 Knowledge / Agentic GraphRAG | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Citation 指向 Chunk，但原文 span 在重切分后已移动”对应的 citation Contract 已能由 `docs/project/knowledge/knowledge-evidence-architecture.md` 说明，问题中的 谁校验 CitationLineage，旧引用如何失效 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Citation 指向 Chunk，但原文 span 在重切分后已移动 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q032 | 03 Knowledge / Agentic GraphRAG | I | I | I | REMAINS_OPEN | NONE | NO | 场景“新 DocumentVersion 只影响一组 Graph 边，不影响全图”对应的 targeted Contract 已能由 `docs/project/knowledge/knowledge-evidence-architecture.md` 说明，问题中的 Projection 如何做定向重建并声明覆盖范围 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 新 DocumentVersion 只影响一组 Graph 边，不影响全图 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q033 | 03 Knowledge / Agentic GraphRAG | I | A | E | EVIDENCE_PENDING | E | YES | 场景“Hybrid 与 Graph 给出互相冲突的 EvidenceCandidate”使 candidate 的 Owner/authority 在 Agent 能否选择一个，还是必须进入冲突处理 时不唯一；Round-005 的同步把边界写清后，剩余成熟度 Gate 是 E 对应的实现或测量，而不是继续接受一个含糊的架构 Contract。 | Hybrid 与 Graph 给出互相冲突的 EvidenceCandidate 的首要架构判断已经完成；当前仍开放的是 E，状态为 EVIDENCE_PENDING，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q034 | 03 Knowledge / Agentic GraphRAG | I | I | I | REMAINS_OPEN | NONE | NO | 场景“IndexVersion 落后 DomainVersion，但召回结果看起来相关”对应的 version Contract 已能由 `docs/project/knowledge/knowledge-evidence-architecture.md` 说明，问题中的 哪个版本屏障允许进入 Plan，旧结果如何标记 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | IndexVersion 落后 DomainVersion，但召回结果看起来相关 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q035 | 03 Knowledge / Agentic GraphRAG | I | E | E | EVIDENCE_PENDING | I | YES | 场景“Graph 成本上升但跨文档任务没有稳定收益”已有可执行的 component Contract，问题中的 谁触发条件降级，如何防止实现惯性 真正缺少的是对质量、效率、成本或归因的受控测量；因此第一阻塞 Gate 是 E，而不是把未证明的收益误写成 I 或 A。 | Graph 成本上升但跨文档任务没有稳定收益 的首要架构判断已经完成；当前仍开放的是 E，状态为 EVIDENCE_PENDING，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q036 | 04 Model Gateway | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Provider-A 超时后切换 Provider-B，B 返回不同 structured output”对应的 provider Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 这是同一 Attempt 的 fallback 还是新的 Attempt，谁归一化 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Provider-A 超时后切换 Provider-B，B 返回不同 structured output 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q037 | 04 Model Gateway | I | I | I | REMAINS_OPEN | NONE | NO | 场景“长 Run 的预算只剩一个并行 Step，但 fallback 需要更多 token”对应的 budget Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 预算由谁裁剪，如何避免 fallback 偷渡新预算 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 长 Run 的预算只剩一个并行 Step，但 fallback 需要更多 token 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q038 | 04 Model Gateway | I | I | I | REMAINS_OPEN | NONE | NO | 场景“模型版本升级后旧 Plan 仍要求旧 Tool Schema”对应的 model Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 兼容窗口如何决定继续、转换或重规划 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 模型版本升级后旧 Plan 仍要求旧 Tool Schema 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q039 | 04 Model Gateway | I | X | X | DEFERRED | I | YES | 场景“Gateway 暂时不可用，但已有可审查的 Plan 和缓存 Observation”的 degraded 设计可以说明，问题中的 降级能否继续，哪些动作必须停止 只有在真实 Provider、Sandbox、滚动部署或资格环境中才能取得证据；因此第一阻塞 Gate 是 X，代码缺口只能作为 secondary gap。 | Gateway 暂时不可用，但已有可审查的 Plan 和缓存 Observation 的首要架构判断已经完成；当前仍开放的是 X，状态为 DEFERRED，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q040 | 04 Model Gateway | I | E | E | EVIDENCE_PENDING | I | YES | 场景“A/B 两个架构使用不同 fallback 链和不同 token 上限”已有可执行的 benchmark Contract，问题中的 如何避免把 Provider 差异误归因于 Runtime 真正缺少的是对质量、效率、成本或归因的受控测量；因此第一阻塞 Gate 是 E，而不是把未证明的收益误写成 I 或 A。 | A/B 两个架构使用不同 fallback 链和不同 token 上限 的首要架构判断已经完成；当前仍开放的是 E，状态为 EVIDENCE_PENDING，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q041 | 05 Memory & Context | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Memory Provider 写入一条后来被 Reviewer 拒绝的经验”对应的 memory Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 下一次 Recall 如何知道它是 rejected proposal 而非事实 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Memory Provider 写入一条后来被 Reviewer 拒绝的经验 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q042 | 05 Memory & Context | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Run Crash 后重放同一个 Step，working context 被再次追加”对应的 context Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 Replay 如何幂等，如何避免上下文越来越长 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Run Crash 后重放同一个 Step，working context 被再次追加 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q043 | 05 Memory & Context | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Matter 权限撤销后旧 Memory 仍能被相似度命中”对应的 memory Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 Recall Gate 在 Provider 前后分别检查什么 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Matter 权限撤销后旧 Memory 仍能被相似度命中 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q044 | 05 Memory & Context | I | X | X | DEFERRED | I | YES | 场景“OpenViking Provider 被替换，旧 Memory 有不同的分层字段”的 provider 设计可以说明，问题中的 哪些语义必须由 Zuno Contract 保持，哪些可以丢弃 只有在真实 Provider、Sandbox、滚动部署或资格环境中才能取得证据；因此第一阻塞 Gate 是 X，代码缺口只能作为 secondary gap。 | OpenViking Provider 被替换，旧 Memory 有不同的分层字段 的首要架构判断已经完成；当前仍开放的是 X，状态为 DEFERRED，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q045 | 05 Memory & Context | I | I | I | REMAINS_OPEN | NONE | NO | 场景“User preference 与当前 Case Fact 冲突”对应的 precedence Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 Planner 如何按 provenance 和 authority 排序 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | User preference 与当前 Case Fact 冲突 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q046 | 05 Memory & Context | I | E | E | EVIDENCE_PENDING | I | YES | 场景“Memory 已 stale 但索引仍然命中”已有可执行的 staleness Contract，问题中的 是降权、拒绝还是要求重新验证，谁做决定 真正缺少的是对质量、效率、成本或归因的受控测量；因此第一阻塞 Gate 是 E，而不是把未证明的收益误写成 I 或 A。 | Memory 已 stale 但索引仍然命中 的首要架构判断已经完成；当前仍开放的是 E，状态为 EVIDENCE_PENDING，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q047 | 05 Memory & Context | I | I | I | REMAINS_OPEN | NONE | NO | 场景“多个 Specialist Agent 共享 Matter Context”对应的 shared Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 谁有写权，如何审计和隔离不同 scope 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 多个 Specialist Agent 共享 Matter Context 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q048 | 05 Memory & Context | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Recall 改变 EvidenceRequirement 的排序”对应的 memory Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 是否必须显式生成 Replan，而不是让 Memory 隐式改图 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Recall 改变 EvidenceRequirement 的排序 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q049 | 06 Agent Core / Planning & Control | I | I | I | REMAINS_OPEN | NONE | NO | 场景“PlanVersion P12 基于 DomainVersion D40，用户提交 D41”对应的 version Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 Replan Barrier 如何处理已完成、运行中和未派发的 Step 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | PlanVersion P12 基于 DomainVersion D40，用户提交 D41 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q050 | 06 Agent Core / Planning & Control | I | E | E | EVIDENCE_PENDING | I | YES | 场景“必需 Worker 超时、可选 Worker 成功”已有可执行的 join Contract，问题中的 Join 是否停止、降级还是进入 Review，谁定义足够 真正缺少的是对质量、效率、成本或归因的受控测量；因此第一阻塞 Gate 是 E，而不是把未证明的收益误写成 I 或 A。 | 必需 Worker 超时、可选 Worker 成功 的首要架构判断已经完成；当前仍开放的是 E，状态为 EVIDENCE_PENDING，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q051 | 06 Agent Core / Planning & Control | I | I | I | REMAINS_OPEN | NONE | NO | 场景“两个 BranchResult 同时提交且顺序不同”对应的 reducer Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 Reducer 如何保证确定性并拒绝过期输入 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 两个 BranchResult 同时提交且顺序不同 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q052 | 06 Agent Core / Planning & Control | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Replan 发生时一个 Step 已发出 Tool 请求”对应的 cancellation Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 哪些 Step 可取消，哪些必须进入 unknown outcome 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Replan 发生时一个 Step 已发出 Tool 请求 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q053 | 06 Agent Core / Planning & Control | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Reflection 发现权限不足但建议新增 Tool”对应的 permission Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 它能修改 Plan 还是只能请求人工决定 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Reflection 发现权限不足但建议新增 Tool 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q054 | 06 Agent Core / Planning & Control | I | I | I | REMAINS_OPEN | NONE | NO | 场景“并行分支同时消耗最后一段 Budget”对应的 budget Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 谁原子化扣减预算，谁裁剪剩余工作 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 并行分支同时消耗最后一段 Budget 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q055 | 06 Agent Core / Planning & Control | I | I | I | REMAINS_OPEN | NONE | NO | 场景“Coordinator 重启时 Checkpoint 只写了一半”对应的 checkpoint Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 Resume 如何找到最后完整控制状态 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Coordinator 重启时 Checkpoint 只写了一半 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q056 | 06 Agent Core / Planning & Control | E | I | I | REMAINS_OPEN | NONE | YES | 场景“Worker 返回未声明的 ReplanRequest”对应的 replan Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 Coordinator 如何校验来源、Schema 和权限 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Worker 返回未声明的 ReplanRequest 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q057 | 06 Agent Core / Planning & Control | E | I | I | REMAINS_OPEN | NONE | YES | 场景“Human Review 修改事实，旧 Plan 仍在队列中”对应的 review Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 Plan 如何变 stale，旧 Proposal 如何被拒绝 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Human Review 修改事实，旧 Plan 仍在队列中 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q058 | 06 Agent Core / Planning & Control | E | I | I | REMAINS_OPEN | NONE | YES | 场景“同一 Step Job 被投递两次且第一次响应丢失”对应的 step Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 Step Attempt 和外部 Receipt 如何去重 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 同一 Step Job 被投递两次且第一次响应丢失 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q059 | 06 Agent Core / Planning & Control | E | E | E | EVIDENCE_PENDING | I | NO | 场景“Join 少一个证据分支但已有足够候选”已有可执行的 evidence Contract，问题中的 谁定义 Evidence Sufficiency，Coordinator 能否自行判断 真正缺少的是对质量、效率、成本或归因的受控测量；因此第一阻塞 Gate 是 E，而不是把未证明的收益误写成 I 或 A。 | Join 少一个证据分支但已有足够候选 的首要架构判断已经完成；当前仍开放的是 E，状态为 EVIDENCE_PENDING，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q060 | 06 Agent Core / Planning & Control | E | I | I | REMAINS_OPEN | NONE | YES | 场景“Tool 返回 outcome_unknown，模型建议立即 retry”对应的 effect Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 Runtime 如何强制先 reconcile 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Tool 返回 outcome_unknown，模型建议立即 retry 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q061 | 06 Agent Core / Planning & Control | E | I | I | REMAINS_OPEN | NONE | YES | 场景“用户取消 Run 后 Worker 返回成功 Proposal”对应的 cancel Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 结果是否保留为候选，谁阻止它进入 Canonical State 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 用户取消 Run 后 Worker 返回成功 Proposal 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q062 | 06 Agent Core / Planning & Control | E | I | I | REMAINS_OPEN | NONE | YES | 场景“LangGraph State 显示 Node 完成但 Domain Transaction 未提交”对应的 state Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 Recovery 谁先读，如何避免假装业务完成 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | LangGraph State 显示 Node 完成但 Domain Transaction 未提交 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q063 | 06 Agent Core / Planning & Control | E | I | I | REMAINS_OPEN | NONE | YES | 场景“两个 Coordinator 因网络分区都认为自己拥有 Run”对应的 controller Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 Single Controller 如何以 CAS 或 lease 防止双重 authority 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 两个 Coordinator 因网络分区都认为自己拥有 Run 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q064 | 07 Capability / Skill | E | I | I | REMAINS_OPEN | NONE | YES | 场景“Event Capability v3 增加字段，旧 Agent 只识别 v2”对应的 capability Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 Provider 如何兼容，Agent 是否需要业务分支 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Event Capability v3 增加字段，旧 Agent 只识别 v2 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q065 | 07 Capability / Skill | E | I | I | REMAINS_OPEN | NONE | YES | 场景“Conflict Provider 置信度下降但仍有新 Evidence”对应的 proposal Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 能否自动形成 ConflictVersion，还是必须 Proposal Admission 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Conflict Provider 置信度下降但仍有新 Evidence 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q066 | 07 Capability / Skill | E | I | I | REMAINS_OPEN | NONE | YES | 场景“Fact-Article Provider 返回不同 StatuteVersion”对应的 legal Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 ApplicableLaw 如何绑定法条版本并阻止混用 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Fact-Article Provider 返回不同 StatuteVersion 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q067 | 07 Capability / Skill | E | I | I | REMAINS_OPEN | NONE | YES | 场景“Similar Case Provider 不可用且任务声明类案不是必需”对应的 capability Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 如何降级而不伪造类案结果 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Similar Case Provider 不可用且任务声明类案不是必需 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q068 | 07 Capability / Skill | E | I | I | REMAINS_OPEN | NONE | YES | 场景“Skill 指示与 Security Policy 冲突”对应的 policy Contract 已能由 `docs/project/agents/agent-platform.md` 说明，问题中的 执行时谁拥有优先级，Skill 能否请求而非授权 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Skill 指示与 Security Policy 冲突 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q069 | 07 Capability / Skill | E | E | E | EVIDENCE_PENDING | I | NO | 场景“同一 Capability 有本地模型和 LLM Provider”已有可执行的 provider Contract，问题中的 怎样保持输入输出契约并公平评估替换收益 真正缺少的是对质量、效率、成本或归因的受控测量；因此第一阻塞 Gate 是 E，而不是把未证明的收益误写成 I 或 A。 | 同一 Capability 有本地模型和 LLM Provider 的首要架构判断已经完成；当前仍开放的是 E，状态为 EVIDENCE_PENDING，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q070 | 08 Tool Runtime | E | I | I | REMAINS_OPEN | NONE | YES | 场景“外部写操作已成功但 HTTP 响应丢失”对应的 unknown Contract 已能由 `docs/project/security/security-architecture.md` 说明，问题中的 Runtime 如何用 ProviderOperationId 避免第二次写 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 外部写操作已成功但 HTTP 响应丢失 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q071 | 08 Tool Runtime | E | I | I | REMAINS_OPEN | NONE | YES | 场景“Approval 在执行前被撤销”对应的 execute-time Contract 已能由 `docs/project/security/security-architecture.md` 说明，问题中的 Sandbox 是否重新授权，旧 PreparedAction 是否作废 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Approval 在执行前被撤销 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q072 | 08 Tool Runtime | E | I | I | REMAINS_OPEN | NONE | YES | 场景“文档中的 Prompt Injection 改写 Tool 参数”对应的 untrusted Contract 已能由 `docs/project/security/security-architecture.md` 说明，问题中的 谁验证参数和 EffectScope，谁把内容标为不可信 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 文档中的 Prompt Injection 改写 Tool 参数 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q073 | 08 Tool Runtime | E | I | I | REMAINS_OPEN | NONE | YES | 场景“同一 EffectReceipt 被两个 Worker 消费”对应的 receipt Contract 已能由 `docs/project/security/security-architecture.md` 说明，问题中的 Receipt identity 和 IdempotencyKey 如何阻止重复 Effect 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 同一 EffectReceipt 被两个 Worker 消费 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q074 | 08 Tool Runtime | E | X | X | DEFERRED | I | YES | 场景“Sandbox 崩溃且 Provider 没有返回结果”的 sandbox 设计可以说明，问题中的 系统怎样进入 reconciliation，而不是直接 retry 只有在真实 Provider、Sandbox、滚动部署或资格环境中才能取得证据；因此第一阻塞 Gate 是 X，代码缺口只能作为 secondary gap。 | Sandbox 崩溃且 Provider 没有返回结果 的首要架构判断已经完成；当前仍开放的是 X，状态为 DEFERRED，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q075 | 08 Tool Runtime | E | I | I | REMAINS_OPEN | NONE | YES | 场景“Tool timeout 与 network deny 同时发生”对应的 failure Contract 已能由 `docs/project/security/security-architecture.md` 说明，问题中的 如何区分 typed failure、未知结果和确定拒绝 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Tool timeout 与 network deny 同时发生 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q076 | 08 Tool Runtime | E | I | I | REMAINS_OPEN | NONE | YES | 场景“Secret rotation 发生在长 Run 中”对应的 secret Contract 已能由 `docs/project/security/security-architecture.md` 说明，问题中的 下一个调用使用哪个 SecretEpoch，旧 lease 是否可读 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Secret rotation 发生在长 Run 中 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q077 | 08 Tool Runtime | E | X | X | DEFERRED | I | YES | 场景“Tool Provider v2 输出字段与 v1 不兼容”的 provider 设计可以说明，问题中的 Worker 能否安全降级，何时必须停止执行 只有在真实 Provider、Sandbox、滚动部署或资格环境中才能取得证据；因此第一阻塞 Gate 是 X，代码缺口只能作为 secondary gap。 | Tool Provider v2 输出字段与 v1 不兼容 的首要架构判断已经完成；当前仍开放的是 X，状态为 DEFERRED，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q078 | 08 Tool Runtime | E | I | I | REMAINS_OPEN | NONE | YES | 场景“用户取消不可逆 Effect，同时 Provider 已收到请求”对应的 cancel-effect Contract 已能由 `docs/project/security/security-architecture.md` 说明，问题中的 取消状态如何表达，不能把 cancel 当作未执行 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 用户取消不可逆 Effect，同时 Provider 已收到请求 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q079 | 08 Tool Runtime | E | I | I | REMAINS_OPEN | NONE | YES | 场景“Effect 成功但 Domain Commit 失败”对应的 effect/domain Contract 已能由 `docs/project/security/security-architecture.md` 说明，问题中的 谁创建 Effect reconciliation，何时允许再次提交业务状态 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Effect 成功但 Domain Commit 失败 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q080 | 09 Security | E | I | I | REMAINS_OPEN | NONE | YES | 场景“用户失去 Matter 权限但 Run 尚未结束”对应的 authorization Contract 已能由 `docs/project/security/security-architecture.md` 说明，问题中的 下一次 Retrieval 和 Tool Call 在哪个 Epoch 重新授权 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 用户失去 Matter 权限但 Run 尚未结束 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q081 | 09 Security | E | I | I | REMAINS_OPEN | NONE | YES | 场景“Prompt Injection 要求把案件内容发往外部 API”对应的 egress Contract 已能由 `docs/project/security/security-architecture.md` 说明，问题中的 Security Boundary 如何阻断并留下可审计拒绝 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Prompt Injection 要求把案件内容发往外部 API 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q082 | 09 Security | E | I | I | REMAINS_OPEN | NONE | YES | 场景“Tenant A 的索引被 Tenant B 查询且文本相似”对应的 tenant Contract 已能由 `docs/project/security/security-architecture.md` 说明，问题中的 哪一层做租户隔离，为什么不能只靠 Prompt 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Tenant A 的索引被 Tenant B 查询且文本相似 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q083 | 09 Security | E | I | I | REMAINS_OPEN | NONE | YES | 场景“PreparedAction 携带旧 Approval 和旧 SecurityEpoch”对应的 approval Contract 已能由 `docs/project/security/security-architecture.md` 说明，问题中的 Queue 中的动作执行前如何失效 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | PreparedAction 携带旧 Approval 和旧 SecurityEpoch 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q084 | 09 Security | E | I | I | REMAINS_OPEN | NONE | YES | 场景“Tool 失败信息包含 Secret，Trace 采样后上传”对应的 secret Contract 已能由 `docs/project/security/security-architecture.md` 说明，问题中的 脱敏在日志、Trace 和 Provider Receipt 哪一层完成 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Tool 失败信息包含 Secret，Trace 采样后上传 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q085 | 09 Security | E | X | X | DEFERRED | I | YES | 场景“Sandbox 进程尝试读取未授权文件”的 sandbox 设计可以说明，问题中的 策略拒绝和容器逃逸证据分别由谁记录 只有在真实 Provider、Sandbox、滚动部署或资格环境中才能取得证据；因此第一阻塞 Gate 是 X，代码缺口只能作为 secondary gap。 | Sandbox 进程尝试读取未授权文件 的首要架构判断已经完成；当前仍开放的是 X，状态为 DEFERRED，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q086 | 09 Security | X | I | I | REMAINS_OPEN | NONE | YES | 场景“用户切换 Tenant 后复用旧 Session Token”对应的 tenant Contract 已能由 `docs/project/security/security-architecture.md` 说明，问题中的 Session、Matter 和 Memory Scope 如何重新绑定 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 用户切换 Tenant 后复用旧 Session Token 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q087 | 09 Security | X | X | X | DEFERRED | I | NO | 场景“Tool Version 升级后旧 Approval 的 action hash 仍相同”的 tool 设计可以说明，问题中的 是否要求重新授权，兼容性由谁证明 只有在真实 Provider、Sandbox、滚动部署或资格环境中才能取得证据；因此第一阻塞 Gate 是 X，代码缺口只能作为 secondary gap。 | Tool Version 升级后旧 Approval 的 action hash 仍相同 的首要架构判断已经完成；当前仍开放的是 X，状态为 DEFERRED，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q088 | 10 Observability & Eval | X | I | I | REMAINS_OPEN | NONE | YES | 场景“Trace 采样丢掉了 Replan 和拒绝 Span”对应的 trace Contract 已能由 `docs/project/eval/legal-eval-and-benchmark.md` 说明，问题中的 如何判断一次 Run 是成功、阻塞还是被安全策略拒绝 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Trace 采样丢掉了 Replan 和拒绝 Span 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q089 | 10 Observability & Eval | X | E | E | EVIDENCE_PENDING | I | YES | 场景“Reviewer 对 Evidence Sufficiency 意见不一致”已有可执行的 reviewer Contract，问题中的 指标如何记录分歧，是否可以直接平均 真正缺少的是对质量、效率、成本或归因的受控测量；因此第一阻塞 Gate 是 E，而不是把未证明的收益误写成 I 或 A。 | Reviewer 对 Evidence Sufficiency 意见不一致 的首要架构判断已经完成；当前仍开放的是 E，状态为 EVIDENCE_PENDING，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q090 | 10 Observability & Eval | X | X | X | DEFERRED | I,E | NO | 场景“A/B/C 中 C 因 Provider outage 没有结果”的 denominator 设计可以说明，问题中的 如何区分 blocked、unavailable 和质量失败 只有在真实 Provider、Sandbox、滚动部署或资格环境中才能取得证据；因此第一阻塞 Gate 是 X，代码缺口只能作为 secondary gap。 | A/B/C 中 C 因 Provider outage 没有结果 的首要架构判断已经完成；当前仍开放的是 X，状态为 DEFERRED，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q091 | 10 Observability & Eval | X | I | I | REMAINS_OPEN | NONE | YES | 场景“同一任务发生三次 retry 和一次 replan”对应的 attempt Contract 已能由 `docs/project/eval/legal-eval-and-benchmark.md` 说明，问题中的 Latency、Cost 和 Model Calls 如何归因而不重复计数 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 同一任务发生三次 retry 和一次 replan 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q092 | 10 Observability & Eval | X | E | E | EVIDENCE_PENDING | I | YES | 场景“Graph 与 Hybrid 都提高最终 LLM Judge 分数但引用变差”已有可执行的 multi-metric Contract，问题中的 哪个指标阻止错误的总体结论 真正缺少的是对质量、效率、成本或归因的受控测量；因此第一阻塞 Gate 是 E，而不是把未证明的收益误写成 I 或 A。 | Graph 与 Hybrid 都提高最终 LLM Judge 分数但引用变差 的首要架构判断已经完成；当前仍开放的是 E，状态为 EVIDENCE_PENDING，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q093 | 10 Observability & Eval | X | E | E | EVIDENCE_PENDING | I | YES | 场景“故障注入只覆盖 API timeout，未覆盖 crash replay”已有可执行的 fault Contract，问题中的 怎样证明 Recovery Contract 而不是只证明重试 真正缺少的是对质量、效率、成本或归因的受控测量；因此第一阻塞 Gate 是 E，而不是把未证明的收益误写成 I 或 A。 | 故障注入只覆盖 API timeout，未覆盖 crash replay 的首要架构判断已经完成；当前仍开放的是 E，状态为 EVIDENCE_PENDING，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q094 | 11 Infrastructure | X | X | X | DEFERRED | I | NO | 场景“Runtime v2 已滚动到 60%，40 分钟 Run 仍只有 v1 Checkpoint”的 checkpoint 设计可以说明，问题中的 部署如何选择兼容读取、暂停、回滚或隔离 只有在真实 Provider、Sandbox、滚动部署或资格环境中才能取得证据；因此第一阻塞 Gate 是 X，代码缺口只能作为 secondary gap。 | Runtime v2 已滚动到 60%，40 分钟 Run 仍只有 v1 Checkpoint 的首要架构判断已经完成；当前仍开放的是 X，状态为 DEFERRED，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q095 | 11 Infrastructure | X | I | I | REMAINS_OPEN | NONE | YES | 场景“Queue drain 后仍有一个 inflight Tool Job”对应的 drain Contract 已能由 `docs/project/deployment/microservice-deployment.md` 说明，问题中的 升级完成的判定是否等待 Effect 对账 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Queue drain 后仍有一个 inflight Tool Job 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q096 | 11 Infrastructure | X | I | I | REMAINS_OPEN | NONE | YES | 场景“Schema 向前兼容但旧 Worker 写入缺少新字段”对应的 schema Contract 已能由 `docs/project/deployment/microservice-deployment.md` 说明，问题中的 服务如何在窗口内读写并最终关闭兼容分支 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | Schema 向前兼容但旧 Worker 写入缺少新字段 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q097 | 11 Infrastructure | X | X | X | DEFERRED | I | NO | 场景“备份恢复后 Domain State 已恢复，Projection 和 Object Artifact 未恢复”的 restore 设计可以说明，问题中的 系统何时允许重新开放 Retrieval 和 WorkProduct 只有在真实 Provider、Sandbox、滚动部署或资格环境中才能取得证据；因此第一阻塞 Gate 是 X，代码缺口只能作为 secondary gap。 | 备份恢复后 Domain State 已恢复，Projection 和 Object Artifact 未恢复 的首要架构判断已经完成；当前仍开放的是 X，状态为 DEFERRED，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q098 | 11 Infrastructure | X | E | E | EVIDENCE_PENDING | I | YES | 场景“Knowledge Worker 占满 CPU，Agent Runtime 长时间饥饿”已有可执行的 resource Contract，问题中的 调度和资源隔离如何保护长 Run 与短请求 真正缺少的是对质量、效率、成本或归因的受控测量；因此第一阻塞 Gate 是 E，而不是把未证明的收益误写成 I 或 A。 | Knowledge Worker 占满 CPU，Agent Runtime 长时间饥饿 的首要架构判断已经完成；当前仍开放的是 E，状态为 EVIDENCE_PENDING，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q099 | 11 Infrastructure | X | I | I | REMAINS_OPEN | NONE | YES | 场景“两个 Worker 都认为自己是队列主消费者”对应的 worker Contract 已能由 `docs/project/deployment/microservice-deployment.md` 说明，问题中的 如何避免 split-brain-like execution 和重复外部动作 不再需要新的架构原则；第一阻塞 Gate 是把版本、失败和恢复路径接入代码、测试或运行记录，所以归 I 而不是 A/E/X。 | 两个 Worker 都认为自己是队列主消费者 的首要架构判断已经完成；当前仍开放的是 I，状态为 REMAINS_OPEN，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
| Q100 | 11 Infrastructure | X | X | X | DEFERRED | I,E | NO | 场景“Production 配置存在但没有 HA、故障注入或 RPO/RTO 结果”的 qualification 设计可以说明，问题中的 部署状态如何保持 NOT_ESTABLISHED 而不是升级宣传 只有在真实 Provider、Sandbox、滚动部署或资格环境中才能取得证据；因此第一阻塞 Gate 是 X，代码缺口只能作为 secondary gap。 | Production 配置存在但没有 HA、故障注入或 RPO/RTO 结果 的首要架构判断已经完成；当前仍开放的是 X，状态为 DEFERRED，因为恢复/实现/测量/外部资格证据尚未跨过该 Gate。 |
