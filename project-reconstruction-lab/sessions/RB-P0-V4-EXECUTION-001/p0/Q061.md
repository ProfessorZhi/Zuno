# Q061 — Prepare / Execute Authorization

- P0 ID: `Q061`
- Architecture Claim: prepare-time 与 execute-time authorization 都必须存在，撤权后 execute 必须 DENY。
- Scope: tenant、scope、capability、SecurityEpoch、secret access、revocation。
- Verification Level: `V3 existing contract only`
- Environment: 当前 Security/Tool batch verifier、Python 3.12；无 execute-side revoke integration。
- Command: `python tools/scripts/verify_security_runtime_batch.py`；`python tools/scripts/verify_tool_runtime_batch.py`（显式源码路径入口）。
- Fixture / Input: default deny、policy version、epoch、approval/security contract fixtures。
- Fault Injected: existing default-deny and stale-epoch model inputs；prepare-allowed→revoke→execute 未能执行。
- Expected: revoked/expired authorization DENY。
- Actual: batch contract PASS；execute-side revocation NOT EXECUTED。
- Exit Code: `0` for existing verifiers。
- Trace: `results/command-log.md#q061`
- State Before: contract fixture state。
- State After: model denies unsafe path。
- Evidence Artifact: existing batch verifier outputs。
- Limitations: 没有真实授权存储、撤权传播和 Tool Gateway 执行路径。
- Cannot Infer: Q061 V4 closure 或生产安全。
- Red Decision: `REQUEST_MORE_EVIDENCE`
- Counter Retest: `NOT_RUN`
- Final Status: `IMPLEMENTATION_DEPENDENT / OPEN`
