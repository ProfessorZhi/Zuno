# Current Runtime Reality

当前仓库的运行状态以 `docs/evidence/` 和 `docs/status/` 为准；Target 文档不能替代运行证据。

需要分别核验：

```text
代码存在
→ 测试通过
→ 本地运行
→ 集成运行
→ Trace/Eval
→ Pilot
→ Production
```

Compose 中多个容器不自动等于微服务；LangGraph、Memory、GraphRAG 代码表面不自动等于历史产品主链路或质量已证明。
