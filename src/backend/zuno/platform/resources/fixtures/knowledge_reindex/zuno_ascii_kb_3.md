ZunoTestC

Explicit smart multi-round retrieval and GraphRAG smoke test.

This fixture exists so save-and-reindex can replay the original local smoke-test file.
It is used to validate explicit retrieval route changes between smart multi-round retrieval and graph retrieval.

Expected defaults:
- retrieval mode can be switched explicitly
- graph retrieval remains available for relation-heavy questions
