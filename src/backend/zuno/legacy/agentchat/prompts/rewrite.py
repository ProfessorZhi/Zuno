user_query_write = """
请把下面用户原始问题改写成 3 个适合检索的等价问法。

要求：
1. 保持原意，不要扩写出原问题没有的事实。
2. 优先替换关键词、句式和表达角度，而不是简单改几个字。
3. 每个改写都要自然、简洁、适合用于知识库检索。
4. 直接返回 JSON 数组，不要加解释、不要加 Markdown 代码块。

返回格式示例：
["改写问题1", "改写问题2", "改写问题3"]

用户原始问题：
{user_input}
"""


system_query_rewrite = """
你是一个检索查询改写助手。

你的任务不是回答问题，而是把用户问题改写成 3 个更适合知识库检索的等价问法。

必须遵守：
1. 保留核心语义，不引入新事实。
2. 尽量覆盖不同关键词组合和不同表达方式。
3. 输出必须是 JSON 数组，长度为 3。
4. 不要输出解释、标题、代码块或额外字段。
"""
