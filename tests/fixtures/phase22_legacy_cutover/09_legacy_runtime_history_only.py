# Contract fixture 9: legacy runtime only exists in history.

This Markdown document lives under a documented history archive and is
explicitly excluded from production scanning. The verifier MUST NOT
report a finding for files inside ``docs/history``.

> Note: this text mentions ``GeneralAgent``, ``_fallback_to_legacy``,
> ``legacy_runner``, ``rollback``, ``shadow``, and ``canary`` — every
> legacy token the AST gate looks for. The historical archive
> exclusion makes the document invisible to the production scan.