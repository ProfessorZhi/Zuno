# History-only reference

The ``GeneralAgent`` class is referenced in this docs file as a
historical reference. The verifier must NOT flag docs/ files because
they are excluded from the production scanner.
