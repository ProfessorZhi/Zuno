name: Security Target Docs

on:
  pull_request:
    branches:
      - main
    paths:
      - "docs/modules/09-security.md"
      - ".agent/modules/09-security.md"
      - "docs/modules/README.md"
      - ".agent/modules/README.md"
      - "tools/scripts/verify_security_target_protocols.py"
      - "tests/repo/test_security_target_protocols.py"
      - ".github/workflows/security-target-docs.yml"
  push:
    branches:
      - main
    paths:
      - "docs/modules/09-security.md"
      - ".agent/modules/09-security.md"
      - "docs/modules/README.md"
      - ".agent/modules/README.md"
      - "tools/scripts/verify_security_target_protocols.py"
      - "tests/repo/test_security_target_protocols.py"
      - ".github/workflows/security-target-docs.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: security-target-docs-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate Security target contracts
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install validation dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pytest

      - name: Verify executable and workflow diff formatting
        run: git diff --check origin/main...HEAD -- '*.py' '*.yml' '*.yaml'

      - name: Compile verifier and tests
        run: |
          python -m py_compile \
            tools/scripts/verify_security_target_protocols.py \
            tests/repo/test_security_target_protocols.py

      - name: Verify Security target protocols
        run: python tools/scripts/verify_security_target_protocols.py

      - name: Verify formal and Agent mirror bytes
        run: cmp docs/modules/09-security.md .agent/modules/09-security.md

      - name: Verify documentation entrypoints and boundaries
        run: |
          python tools/scripts/verify_docs_entrypoints.py
          python .agent/scripts/verify_agent_system.py
          python .agent/scripts/verify_doc_boundaries.py

      - name: Run focused repository tests
        run: |
          pytest -q \
            tests/repo/test_security_target_protocols.py \
            tests/repo/test_docs_entrypoints.py \
            -p no:cacheprovider
