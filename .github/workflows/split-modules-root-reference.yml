name: Split modules root reference

on:
  push:
    branches: [docs/split-human-engineering-references]
    paths:
      - "tools/scripts/_split_modules_root_reference.py"
      - ".github/workflows/split-modules-root-reference.yml"

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: docs/split-human-engineering-references
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Split modules root reference
        run: |
          python tools/scripts/_split_modules_root_reference.py
          git diff --check
      - name: Commit split
        run: |
          if git diff --quiet; then
            echo "No split changes needed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs: move cross-module engineering material to reference"
          git push origin HEAD:docs/split-human-engineering-references
