from __future__ import annotations

from pathlib import Path


ROOT = Path(__file__).resolve().parents[2]
PATH = ROOT / "docs/project/README.md"
START = "## 通用平台之后仍然留下的法律责任"
END = "## 项目真实走过的阶段"

NEW_SECTION = """## 通用平台之后仍然留下的法律责任

成熟的通用 Agent 平台已经能提供用户入口、会话、工作流、模型调用、RAG、Tool Calling、页面交互和部分运行观测。简单法律问答如果只需要受控检索、稳定引用和一次生成，这些能力已经足够，Zuno 没有理由重新造一套宿主。

这也是 Zuno 与通用平台更合理的关系。通用宿主可以继续承担 UI、会话和通用编排，法律项目只补充宿主无法替业务决定的部分。只有任务真的跨越长时间运行、正式结果、人工权威和现实副作用时，更强的运行控制与领域状态才开始有价值。

边界由业务责任决定，而不是由框架选择决定。LangGraph、长期 Memory、GraphRAG 或某一种模型可以替换；材料在什么时候足以支持当前任务、机器输出什么时候可以进入正式成果、旧成果在新证据出现后是否仍有效，这些长期语义不能随着框架一起漂移。

通用能力优先复用以后，法律工作仍留下六类项目必须解释清楚的问题。

**材料需要稳定身份，也需要面向任务的就绪判断。** 文件上传成功只能说明材料到达。OCR、切分和检索表示可以继续构建，某些派生结果还可以在算法升级后重做。真正开始回答一个具体法律问题以前，系统还要判断缺失材料是否会影响当前结论。查询某一条合同条款与确认全案违约金额，对“材料已经够用”的要求并不相同。

**机器输出先保持候选身份。** 检索、模型和专业算法都会犯错，也可能只覆盖问题的一部分。它们可以帮助专业人员形成判断，却不能因为计算链路成功就自动升级成正式证据或长期工作成果。需要长期保存和继续流转的结果，要经过业务规则与必要的人审才能正式成立。

**正式成果必须记住自己当时依据了什么。** 检索索引可以重建，历史成果所采用的材料版本、稳定位置和专业判断不能跟着索引重建一起漂移。新证据进入以后，旧成果也可能转为需要复核；系统应保留历史版本并传播失效，而不是覆盖过去或默认全案重算。

**现实副作用需要单独确认。** 纯计算失败通常可以重新执行，向外围系统创建记录、提交结果或发送不可逆通知则不同。请求超时只证明本地没有拿到确定响应，远端可能尚未执行，也可能已经成功。系统需要先确认现实结果，再决定是否继续，避免把网络失败直接变成重复业务动作。

**研究成果要经过工程资格才能成为长期能力。** 研究模型、规则脚本、LLM 和外部 API 都可能完成同一类专业任务。业务系统需要依赖稳定的专业承诺，而不是依赖某一篇论文或某个 SDK；新的实现先证明接口和基础语义，再用真实任务评价它是否适合当前数据、任务范围和质量要求。

**专业质量需要独立证据。** 模型返回成功、JSON Schema 正确或 Top-K 有结果，只能证明计算链路完成。法律质量还要看证据是否充分、引用是否正确、是否存在无依据主张、结论是否适用、人工是否接受，以及恢复、时延和成本是否在可接受范围内。GraphRAG、长期 Memory、Reflection、Specialist Agent 和 Native Runtime 也只有在这些测量中稳定带来收益时才值得保留。

六类问题共享同一个原则：基础设施和通用平台负责提供机制，Zuno 只拥有法律业务必须长期解释的语义与恢复责任。具体的事实 Authority、完成证明和跨边界 Contract 由后面的 Target Architecture 继续定义。

## 任务复杂度决定需要多少 Zuno

用户只问“合同第 8 条约定了什么”时，最短路径仍然是权限检查、材料读取、检索、引用和生成。任务结束后没有正式成果需要长期维护，也没有现实副作用需要确认，普通受控 RAG 就可以保持简单。

多材料争议分析开始要求更长的责任链。系统要先判断当前问题是否拥有足够材料，再从材料中形成候选事实、事件和证据，由专业能力和模型协助比较与分析；需要成为长期工作成果的内容再进入业务接纳和必要人审。服务重启、补交证据或权限变化以后，系统还要能够说明哪些结果仍然成立，以及应该从什么事实继续。

如果任务继续向外围系统写入结果，问题又增加一层。系统要在动作发生前确认当前授权和必要审批，动作发生后确认现实结果；网络超时不能直接触发盲目重试。复杂度由这些业务边界逐层增加，而不是由“用了多少 Agent”决定。

这种递进关系并不意味着每个请求都要依次经过九个责任域。九个逻辑责任域只是告诉系统：当某类事实需要长期保存和恢复时，谁拥有最终解释权。简单任务可以绕过大量复杂机制，复杂任务也只激活当前真正需要的责任。

同一组法律责任可以落在三种产品形态。

**通用宿主 + 法律 Skills / Capabilities。** 对简单问答、轻量检索和少量专业能力，这通常已经足够。成熟宿主继续承担会话、页面和通用工作流，法律能力只提供专业处理。

**通用宿主 + Zuno Legal Backend。** 当材料版本、正式成果、长期失效和恢复开始成为业务要求时，通用宿主仍可负责产品入口，Zuno 后端负责这些法律语义。这个形态保留成熟宿主能力，也避免为了一个专业后端接管整个平台。

**Zuno Native Runtime + 一等领域状态。** 只有任务确实需要动态计划、长期等待、可恢复的多步骤执行、安全并行、人工中断和持续授权时，原生运行时的额外成本才有依据。

三种形态没有从低到高的等级关系。选择取决于法律质量、人工接受率、故障恢复、时延、Token、成本和维护复杂度。中间形态已经满足约束时，继续建设完整原生运行时就是多余复杂度。

复杂机制因此都需要退出条件。GraphRAG 如果长期不能优于更简单的 Hybrid Retrieval，就不应留在默认主路径；长期 Memory 没有稳定收益时，应缩回短期上下文；Reflection、Specialist 或 Native Runtime 没有改善质量或恢复正确性时，也应关闭或降级。已经实现只能证明代码存在，不能证明设计应该永久存在。

逻辑责任与物理部署同样保持分离。九个责任域不等于九个微服务；在独立扩缩容、安全隔离、故障半径、团队边界或部署生命周期形成真实需求以前，它们可以继续共处模块化 Python 后端和必要 Worker 中。系统先保护事实边界，再根据证据决定是否拆服务。

"""


def main() -> None:
    text = PATH.read_text(encoding="utf-8")
    if text.count(START) != 1 or text.count(END) != 1:
        raise RuntimeError("project positioning markers must each occur exactly once")
    start = text.index(START)
    end = text.index(END)
    if start >= end:
        raise RuntimeError("project positioning markers out of order")
    rewritten = text[:start] + NEW_SECTION + text[end:]
    PATH.write_text(rewritten, encoding="utf-8")

    after = PATH.read_text(encoding="utf-8")
    for required in (
        "通用能力优先复用",
        "机器输出先保持候选身份",
        "现实副作用需要单独确认",
        "研究成果要经过工程资格",
        "专业质量需要独立证据",
        "通用宿主 + 法律 Skills / Capabilities",
        "通用宿主 + Zuno Legal Backend",
        "Zuno Native Runtime + 一等领域状态",
        "九个责任域不等于九个微服务",
    ):
        if required not in after:
            raise RuntimeError(f"rewritten project positioning missing: {required}")


if __name__ == "__main__":
    main()
