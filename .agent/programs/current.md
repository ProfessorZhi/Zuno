# 当前程序

当前没有 active program。

最近完成并归档：

- `docs/history/programs/zuno-workflow-doc-system-v1/`
- `docs/history/programs/zuno-target-architecture-refresh-v1/`

## 等待打开

下一个候选 program 是 `zuno-repo-layout-cleanup-v1`，仍然只是 queued draft：

- `.agent/architecture/future/programs/zuno-repo-layout-cleanup-v1/`

打开它时，必须先把对应 roadmap 和 `PHASE*.md` 文件迁入 `.agent/programs/`，再从 `PHASE01` 开始执行。

## 当前边界

- 不继续追加 Program 1 或 Program 2 的 phase。
- 不把 queued draft 当成 active program。
- 不实施 runtime、API、DB schema、frontend 或 eval baseline 修改。
- 不把目标架构能力写成 Current，除非代码和测试已经证明。
