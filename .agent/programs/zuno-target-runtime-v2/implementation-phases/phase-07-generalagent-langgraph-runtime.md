# Phase 07 GeneralAgent LangGraph Runtime

## Status

Future phase. Not in the first implementation slice.

## Goal

Make the runtime graph explicitly express
`prepare_context -> agent_loop -> post_turn_commit` without changing behavior
blindly.
