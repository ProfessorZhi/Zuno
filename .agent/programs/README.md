# Agent 执行计划

`.agent/programs/` 只存放当前可执行计划。

## 当前状态

当前没有 active program。

最新完成：`zuno-repo-layout-cleanup-v1` / Program 3 final alias surface closure。`src/backend/zuno` 根目录已只保留 `__init__.py`、`main.py` 和六层目录，旧 public import path 由 `platform/compatibility/legacy_aliases.py` 注册兼容。

## 当前入口

- [current.md](current.md)：当前程序状态。
- [implementation-roadmap.md](implementation-roadmap.md)：当前 program 状态与 queued program 路线图。
- [closure-checklist.md](closure-checklist.md)：通用收口清单。

## 历史边界

Program 3 PHASE01-15、Directory Surface Alignment V1 和 final alias surface closure 已归档到：

- `docs/history/programs/zuno-repo-layout-cleanup-v1/`

Program 4/5 仍是 queued draft / not active。
