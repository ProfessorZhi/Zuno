# Agent Notes

This directory is reserved for durable non-secret Agent notes that are safe to
share in the repository.

Local scratch notes remain ignored by default. Do not commit API keys, tokens,
machine-local paths, logs, or secrets here.
