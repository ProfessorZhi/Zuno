# Open Questions

1. Confirm Zuno-native official-compatible GraphRAG first?
2. Confirm no long-term Domain Pack shim?
3. Confirm Basic = BM25 + vector + fusion + rerank?
4. Confirm first-level query methods do not include `community`?
5. Confirm Java backend is the business truth layer?
6. Should Java/Python integration prioritize HTTP, gRPC, or message queue?
7. Should microservices remain boundary design only for now?
8. Should multi-agent stay limited to complex tasks?
9. Should contract review be the first GraphRAG Project example?
10. Should tenant fields enter architecture before implementation?
11. Should DRIFT be default-visible or advanced slow mode?
12. Should Prompt Tuning v1 be manual versioning first or auto tuning now?
13. Which current `domain_pack_id` storage paths can be migrated without data
    compatibility requirements?
14. Should GraphRAG project artifacts live inside database rows, object storage,
    or a repository-like project directory?
