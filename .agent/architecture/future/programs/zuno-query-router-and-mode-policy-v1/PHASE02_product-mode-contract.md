# PHASE02：Product Mode Contract
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

定义普通模式、增强模式和自动模式的 DTO、UI 文案、预算边界和 fallback 语义。

## 验收

普通模式固定低预算 basic；增强模式允许 Agentic RAG；自动模式先判断 direct / basic / enhanced。
