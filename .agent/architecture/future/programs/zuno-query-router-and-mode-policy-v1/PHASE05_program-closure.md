# PHASE05：Program Closure
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

同步 docs、`.agent`、tests、verifier 和 history，确认没有把目标行为写成 Current。

## 验收

program 归档到 `docs/history/programs/`，`.agent/programs/` 回到 no-active 或明确下一个 active program。
