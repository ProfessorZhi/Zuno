# PHASE01：Current Router Audit
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

审计当前 mode、query_method、legacy route、frontend contract、trace metadata 和 tests。

## 验收

列出当前 direct / basic / enhanced / auto 的真实入口、缺口、兼容 alias、风险和最小实现顺序。
