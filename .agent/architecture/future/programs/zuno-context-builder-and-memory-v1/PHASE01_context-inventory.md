# PHASE01：Context Inventory
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

审计当前 ContextOrchestrator、ModelContextPacket、memory surfaces、tool results 和 knowledge evidence 注入路径。

## 验收

输出 current owner、target owner、缺口、风险和 stop conditions。
