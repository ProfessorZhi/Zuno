# PHASE03：Memory Taxonomy Surfaces
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

建立短期状态、工作记忆、语义记忆、情节记忆、程序性记忆的薄入口和写入边界。

## 验收

每类记忆有 owner、read/write policy、source ids 和 focused tests；不引入 eager heavy runtime。
