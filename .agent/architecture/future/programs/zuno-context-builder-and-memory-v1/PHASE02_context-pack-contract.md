# PHASE02：Context Pack Contract
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

定义 Context Pack 的字段、来源、裁剪理由、token budget 和 trace metadata。

## 验收

Agent 能看到最小但足够的 context；trace 能解释哪些 memory / evidence / ToolCard 被选中或剔除。
