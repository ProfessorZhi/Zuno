# PHASE01：Visual Surface Audit
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

审计当前 `docs/architecture/architecture.md`、`docs/architecture/architecture.html` 和 render 脚本的展示问题。

## 验收

列出信息层级、视觉层级、同步风险、GitHub 入口问题。
