# PHASE02：Information Architecture And Style
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

确定三张图的页面结构、色彩、说明层级和 responsive 行为。

## 验收

形成轻量视觉规范，不引入重前端站点。
