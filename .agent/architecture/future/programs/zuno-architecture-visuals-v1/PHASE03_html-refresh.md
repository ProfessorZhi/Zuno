# PHASE03：HTML Refresh
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

重做架构 HTML 页面，使 Current Runtime、Target Runtime、Maintenance Workflow 更清楚。

## 验收

HTML 由 render 脚本生成，文字不遮挡，入口可发现。
