# PHASE03：Knowledge Extractor Configs
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

让知识库或 GraphRAG project 能选择 extractor / model / cost policy 配置。

## 验收

配置边界清楚，默认行为兼容，trace 能显示 resolved strategy。
