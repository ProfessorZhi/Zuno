# PHASE04：Memory / Capability / Trace Hardening
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

强化 Memory approval、Capability execution policy、Evidence / Trace / Eval 的 runtime 闭环。

## 验收

关键决策可 trace，风险操作有 policy，focused tests 覆盖。
