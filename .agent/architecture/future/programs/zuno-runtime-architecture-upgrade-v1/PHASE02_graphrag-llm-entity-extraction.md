# PHASE02：GraphRAG LLM Entity Extraction
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

实现可配置的 LLM entity extraction runtime slice，避免默认依赖正则/规则匹配。

## 验收

有 extractor strategy、fallback、trace metadata、focused tests。
