# PHASE05：Eval / Trace Closure
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

为 runtime 升级建立 eval 和 trace 证据闭环。

## 验收

有 focused eval、trace fixture、public evidence summary，不把 smoke 写成 full closure。
