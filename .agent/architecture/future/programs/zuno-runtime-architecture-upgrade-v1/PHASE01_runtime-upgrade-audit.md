# PHASE01：Runtime Upgrade Audit
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

审计 GraphRAG、Knowledge、Memory、Capability、Trace 当前实现和测试缺口。

## 验收

输出实现顺序、风险、eval 需求和 stop conditions。
