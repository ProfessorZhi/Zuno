# Local Agent State

This directory is the only place for Agent files that should stay local.

- `logs/`: local run logs and command output
- `tmp/`: temporary scratch files and generated intermediates
- `secrets/`: local credentials or private config; never commit real contents
- `notes/`: private local notes and drafts

Only this README and empty placeholder files are tracked. Real local contents
inside these folders are ignored by Git.
