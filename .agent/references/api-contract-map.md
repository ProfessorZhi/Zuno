# API Contract Map

## Purpose

Map public contract changes across backend and frontend.

## Current Paths

- Backend routes: `src/backend/zuno/api/`
- Backend schemas: `src/backend/zuno/schema/`
- Frontend API clients: `apps/web/src/apis/`
- Frontend utilities/types: `apps/web/src/utils/`
- Contract tests: `tests/` and `tests/compat/`

## Current Direction

Target public GraphRAG fields use GraphRAG Project and Query Method language.
Domain Pack fields are migration-only unless explicitly required by current
runtime compatibility.
