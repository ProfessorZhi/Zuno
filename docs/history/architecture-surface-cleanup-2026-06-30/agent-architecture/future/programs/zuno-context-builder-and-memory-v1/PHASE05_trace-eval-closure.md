# PHASE05：Trace Eval Closure
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

用 trace fixture 和 focused eval 证明 Context Builder 与 Memory policy 可解释。

## 验收

docs / `.agent` / tests / verifier 同步，program 归档后 `.agent/programs/` 不保留 phase 文件。
