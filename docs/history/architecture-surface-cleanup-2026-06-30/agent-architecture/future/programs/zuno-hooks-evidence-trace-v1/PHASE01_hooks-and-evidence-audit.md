# PHASE01：Hooks And Evidence Audit
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

审计当前 trace、citation、evidence bundle、tool call policy、runtime events 和 artifact path。

## 验收

输出缺口、风险、现有 tests、需要新增的 event schema 和 stop conditions。
