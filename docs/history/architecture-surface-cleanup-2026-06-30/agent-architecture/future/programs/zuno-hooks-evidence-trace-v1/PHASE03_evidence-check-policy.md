# PHASE03：Evidence Check Policy
> 状态：queued draft / not active。不要直接执行本文；打开该 program 时，必须先迁入 `.agent/programs/`，并从 `PHASE01` 开始。

## 目标

定义 evidence coverage、citation coverage、conflict detection、insufficient evidence 和 retry/fallback 语义。

## 验收

回答前必须能解释 pass / insufficient；增强模式允许 rewrite / add channel / requery，普通模式保持低预算。
