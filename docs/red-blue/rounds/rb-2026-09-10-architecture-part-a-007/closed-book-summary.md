# Closed-book Gate Summary

Before the adversarial Round, the current Human Part A surfaces were read without using Engineering Reference as a teaching aid.

The reader could reconstruct the main system story from the Human documents alone:

- controlled RAG remains the baseline for simple questions;
- material availability is task-specific rather than a global upload/index flag;
- research implementations need stable professional semantics before they become replaceable capabilities;
- machine output becomes long-lived business truth only through the Domain boundary;
- Runtime recovery must defer to stronger durable Owner facts;
- external timeout can leave real-world outcome unknown;
- authorization is re-evaluated at new protected actions during long tasks;
- model routing becomes independent when quality, egress, budget, and provider failure vary;
- implemented complexity remains subject to baseline, ablation, and deletion tests.

The gate passed for proceeding to Red / Blue. The later Red / Blue round found one narrower global narrative gap around deriving Application & Integration before the nine-domain summary; that finding is recorded separately in `findings.md`.

This summary is a review artifact, not Architecture Truth or Current Evidence.
